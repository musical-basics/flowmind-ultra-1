use std::sync::Arc;
use tokio::sync::Mutex;

// Scaffold: whisper-rs contextual bindings
pub struct WhisperContext;
pub struct WhisperState;

pub fn process_transcription() -> String {
    String::new()
}
